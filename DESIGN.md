# MILI AI Design System Draft

> Status: Draft v0.1  
> Updated: 2026-07-23  
> Scope: 학습자용 웹 서비스의 홈, VOD, PBL, 커뮤니티, 개인 대시보드, 프로필, 역량진단, 로드맵  
> Reference: [Hubtown](https://hubtown.co.in/) 및 제공된 MILI AI 메뉴 맵·메인 화면 시안

## 0. Design Thesis

MILI AI는 AI 학습 콘텐츠를 전시하는 사이트가 아니라, 학습자가 **다음 행동을 이해하고 실행하며 피드백을 받아 성장 증거를 축적하는 학습 운영 시스템**이다.

디자인은 다음 순서로 사용자의 질문에 답해야 한다.

1. 지금 무엇을 해야 하는가?
2. 왜 이 활동을 해야 하는가?
3. 완료까지 얼마나 남았는가?
4. 막혔을 때 어디에서 도움을 받는가?
5. 완료한 활동이 역량과 프로젝트 결과물에 어떻게 연결되는가?

Hubtown의 시네마틱한 전체 화면, 얇은 시스템 프레임, 공간감, 중심 오브젝트를 참고하되 그대로 복제하지 않는다. MILI AI에서는 이를 **Learning Mission Control**이라는 학습 문법으로 번역한다.

- 배경은 세계관과 방향을 만든다.
- 전경의 패널은 판단과 실행을 돕는다.
- 네온 액센트는 “클릭할 수 있음”과 “지금 중요한 상태”에만 사용한다.
- 지도와 탐사 은유는 학습 순서와 진척을 설명할 때만 사용한다.
- 모든 장식보다 학습 상태, 마감, 피드백, 다음 행동의 판독성이 우선한다.

---

## 1. Product Identity

### 1.1 Product promise

**Make AI Achievable — 배운 것을 수행하고, 수행한 것을 역량으로 증명한다.**

MILI AI의 핵심 가치는 다음 세 축으로 정의한다.

- **Guided:** 학습자가 현재 위치와 다음 단계를 잃지 않는다.
- **Applied:** VOD 시청에서 끝나지 않고 코딩, 퀴즈, AI 채팅, 미션, 팀 구성, 동료평가, 결과물 제출로 이어진다.
- **Evidenced:** 완료율, 피드백, 배지, 스킬, 프로젝트 결과물, 역량진단 결과가 성장의 증거로 축적된다.

### 1.2 Primary user loop

`진단/목표 설정 → VOD로 이해 → 퀴즈·코딩으로 확인 → PBL로 적용 → 동료·리더·AI 피드백 → 역량/로드맵 갱신 → 다음 학습`

화면별 역할은 이 루프 안에서 정의한다.

| Surface | 사용자의 주 질문 | 디자인 우선순위 |
|---|---|---|
| 홈 | 오늘 무엇을 이어서 해야 하지? | 단일 Primary Next Action |
| VOD 목록/상세 | 무엇을 배우고 어디까지 했지? | 진도, 예상 시간, 선수 관계 |
| VOD 학습 | 이해했는지 어떻게 확인하지? | 영상-자료-퀴즈-코딩의 순서 |
| PBL 목록/상세 | 어떤 문제를 왜 해결하지? | 목표, 역할, 산출물, 일정 |
| PBL 학습 | 지금 팀이 해야 할 일은? | 미션, 팀 상태, 피드백, 제출 |
| 커뮤니티 | 질문하거나 참고할 내용은? | 맥락, 답변 상태, 신뢰도 |
| 개인 대시보드 | 어떻게 성장하고 있지? | 증거 기반 진행/성과 요약 |
| 역량진단·로드맵 | 다음 성장 경로는 무엇이지? | 격차, 추천 이유, 다음 단계 |

### 1.3 Voice and terminology

- 한국어를 기본으로 하고 꼭 필요한 제품명·기술명만 영문을 병기한다.
- 군사적 명령보다 **탐사, 여정, 미션, 베이스, 체크포인트**처럼 도전적이되 위협적이지 않은 어휘를 사용한다.
- 실패는 “탈락”이 아니라 “다시 시도할 수 있는 학습 신호”로 표현한다.
- 버튼은 행동으로 시작한다: `이어서 학습하기`, `미션 제출하기`, `피드백 확인하기`.
- 모호한 CTA인 `확인`, `진행`, `클릭하기`, `탐사하기`는 실제 결과가 불분명할 때 사용하지 않는다.
- 레벨·XP는 보조 동기 장치다. 실질적 학습 성과보다 앞세우지 않는다.

---

## 2. Visual Concept

### 2.1 Concept: Learning Mission Control

시각 체계는 세 개의 레이어로 구성한다.

1. **World Layer** — 어두운 지형, 네트워크, 좌표, 흐름. 학습 여정의 규모와 가능성을 전달한다.
2. **Work Layer** — 불투명도가 높은 패널, 목록, 에디터, 영상, 제출 영역. 실제 읽기와 작업을 담당한다.
3. **Signal Layer** — 네온 라임, 블루, 상태 아이콘, 포커스 링. 현재 행동과 시스템 피드백을 표시한다.

### 2.2 Reference translation

| Hubtown/시안에서 관찰한 문법 | MILI AI 적용 | 적용하지 않는 경우 |
|---|---|---|
| 한 화면을 채우는 시네마틱 공간 | 온보딩, 홈 상단, 로드맵의 세계관 배경 | 긴 글, 퀴즈, 코딩, 제출, 설정 |
| 중심 오브젝트와 주변 UI 프레임 | 오늘의 미션 또는 현재 여정을 중심에 배치 | 여러 카드가 동등하게 중요한 대시보드 |
| 얇은 라인, 좌표, 작은 시스템 라벨 | 섹션 구분, 메타데이터, 진행 경로 | 본문이나 핵심 안내를 작게 표시할 때 |
| 강한 단일 포인트 컬러 | Primary CTA, 현재 단계, 선택 상태 | 모든 카드·아이콘·차트에 반복 |
| 탐색형 인터랙션 | 로드맵, 프로젝트 단계 탐색 | 필수 메뉴를 숨기거나 발견을 어렵게 할 때 |

### 2.3 Visual density

- 홈은 `cinematic + focused`, 학습 수행 화면은 `quiet + operational`, 대시보드는 `structured + comparable`을 기본 밀도로 한다.
- 한 뷰포트에서 시각적으로 가장 강한 요소는 최대 1개다.
- Primary CTA는 한 작업 영역에 1개만 둔다.
- 배경 이미지 위 텍스트는 항상 독립된 scrim 또는 불투명 패널 위에 놓는다.
- 글래스 효과는 깊이 표현용이지 텍스트 대비를 희생하는 면허가 아니다.

---

## 3. Tokens

토큰 이름은 시각값이 아니라 역할을 나타낸다. 브랜드 색상을 직접 컴포넌트에 하드코딩하지 않는다.

### 3.1 Color

#### Foundation

| Token | Value | Role |
|---|---:|---|
| `color.bg.canvas` | `#050806` | 앱 기본 배경 |
| `color.bg.world` | `#081309` | 시네마틱 배경 위 기본 톤 |
| `color.surface.1` | `#0C120E` | 기본 작업 패널 |
| `color.surface.2` | `#121A15` | 중첩 패널·카드 |
| `color.surface.3` | `#1A241D` | hover/선택 전 단계 |
| `color.border.subtle` | `#29332C` | 기본 구분선 |
| `color.border.strong` | `#445148` | 입력·중요 경계 |
| `color.text.primary` | `#F4F7F4` | 제목·본문 주요 텍스트 |
| `color.text.secondary` | `#B4BDB6` | 설명·메타데이터 |
| `color.text.tertiary` | `#7F8A82` | 비활성 보조 정보 |
| `color.action.primary` | `#B7F52A` | 주 행동·현재 단계 |
| `color.action.primary-hover` | `#C8FF4A` | hover |
| `color.action.on-primary` | `#111807` | 라임 배경 위 텍스트 |
| `color.accent.ai` | `#54C7FF` | AI 안내·AI 생성/피드백 |
| `color.accent.reward` | `#FFD25A` | 배지·보상, 제한적으로 사용 |
| `color.status.success` | `#4ED58A` | 완료·통과 |
| `color.status.warning` | `#FFB84D` | 마감 임박·주의 |
| `color.status.danger` | `#FF6B6B` | 오류·중요 실패 |
| `color.focus` | `#8EDCFF` | 키보드 포커스 링 |

#### Usage rules

- `action.primary`는 브랜드 장식색이 아니라 **다음 행동과 현재 선택 상태**에만 쓴다.
- 한 화면에서 라임이 차지하는 면적은 대략 10% 이하로 유지한다. 넓은 네온 배경은 랜딩/캠페인 장면에만 허용한다.
- 성공과 Primary Action을 같은 라임으로 표현하지 않는다. 완료는 `status.success`와 체크 아이콘/텍스트를 함께 쓴다.
- AI가 만든 내용은 `accent.ai`와 `AI 생성` 라벨을 함께 사용한다. 색만으로 사람/AI 출처를 구분하지 않는다.
- 위험, 마감, 정답/오답은 색 외에 아이콘과 문구를 함께 제공한다.
- 이미지가 포함된 카드의 본문 영역은 최소 `rgba(5, 8, 6, 0.88)` 수준의 scrim을 갖는다.
- 실제 구현 시 모든 텍스트/배경 조합을 WCAG 대비 검사로 검증한다.

### 3.2 Typography

#### Font family

```css
--font-sans: "Pretendard Variable", "Noto Sans KR", system-ui, sans-serif;
--font-display: "Pretendard Variable", "Noto Sans KR", system-ui, sans-serif;
--font-mono: "IBM Plex Mono", "SFMono-Regular", Consolas, monospace;
```

- 한국어 학습 문맥의 판독성을 위해 본문과 UI는 Pretendard/Noto Sans KR 계열을 사용한다.
- 디스플레이 폰트도 별도의 장식 서체보다 같은 산세리프의 무게와 크기로 차이를 만든다.
- Mono는 탐사 ID, 코드, 시간, 짧은 수치 라벨에만 사용한다. 긴 한국어 본문에 사용하지 않는다.

#### Type scale

| Token | Size / Line-height | Weight | Usage |
|---|---|---:|---|
| `type.display` | `clamp(40px, 5vw, 72px) / 1.08` | 700 | 랜딩 메시지 전용 |
| `type.h1` | `clamp(32px, 4vw, 48px) / 1.15` | 700 | 페이지 제목 |
| `type.h2` | `28px / 1.25` | 700 | 주요 섹션 |
| `type.h3` | `22px / 1.35` | 650 | 카드·패널 제목 |
| `type.body-lg` | `18px / 1.6` | 400 | 핵심 안내 |
| `type.body` | `16px / 1.6` | 400 | 기본 본문 |
| `type.body-sm` | `14px / 1.5` | 400 | 메타데이터 |
| `type.label` | `13px / 1.3` | 600 | 버튼·필드 라벨 |
| `type.caption` | `12px / 1.4` | 500 | 제한적 보조 라벨 |

- 학습 본문은 데스크톱에서도 16px 미만으로 내리지 않는다.
- 영문 시스템 라벨의 대문자와 넓은 자간은 짧은 섹션 라벨에만 쓴다.
- 한국어 제목에 과도한 자간을 적용하지 않는다.
- 숫자 진행률은 크기를 키울 수 있지만 의미 라벨과 같은 시야 안에 둔다.

### 3.3 Spacing

4px 기반의 제한된 스케일을 사용한다.

| Token | Value | Typical usage |
|---|---:|---|
| `space.1` | `4px` | 아이콘 내부 보정 |
| `space.2` | `8px` | 아이콘-라벨, 작은 인라인 간격 |
| `space.3` | `12px` | 작은 컨트롤 내부 |
| `space.4` | `16px` | 기본 컴포넌트 간격 |
| `space.5` | `20px` | 카드 내부 그룹 |
| `space.6` | `24px` | 카드 패딩 |
| `space.8` | `32px` | 섹션 내 그룹 |
| `space.10` | `40px` | 큰 패널 패딩 |
| `space.12` | `48px` | 섹션 간격 |
| `space.16` | `64px` | 페이지 상단/대형 섹션 |
| `space.20` | `80px` | 랜딩 장면 전용 |

관련 정보는 가깝게, 다른 작업은 멀리 둔다. 카드 안의 제목-설명 간격보다 카드와 카드 사이 간격이 반드시 커야 한다.

### 3.4 Shape, border, elevation, motion

```css
--radius-control: 10px;
--radius-card: 16px;
--radius-panel: 24px;
--radius-pill: 999px;

--border-subtle: 1px solid var(--color-border-subtle);
--shadow-panel: 0 16px 48px rgb(0 0 0 / 32%);

--duration-fast: 120ms;
--duration-base: 200ms;
--duration-slow: 360ms;
--ease-standard: cubic-bezier(.2, .8, .2, 1);
```

- 반경은 역할별로 고정한다. 같은 계층에서 임의의 반경을 섞지 않는다.
- glow는 Primary CTA의 hover, 현재 로드맵 노드, 완료 순간에만 약하게 사용한다.
- 정보 카드 전체에 강한 그림자나 빛 번짐을 반복하지 않는다.

---

## 4. Information Architecture and Layout Grammar

### 4.1 Recommended top-level navigation

제공된 메뉴 맵의 기능은 다음 5개 사용자 목적 아래 재구성한다.

1. **홈** — 오늘의 다음 행동, 알림, 주간 상태
2. **학습** — VOD 목록/상세/학습, 관심 강의, 최근 학습
3. **프로젝트** — PBL 목록/상세/미션/팀/동료평가/산출물
4. **커뮤니티** — 공지, Q&A, FAQ, 뉴스
5. **나** — 대시보드, 내 강의, 작성 글, 프로필, 배지/스킬/크레딧, 역량진단, 로드맵, 설정

`소개`와 공개 콘텐츠는 로그인 전 글로벌 내비게이션에 둔다. 회원가입/로그인/탈퇴는 학습 메뉴와 섞지 않는다. 관리자 시스템은 학습자 IA와 별도의 셸·권한·URL 영역으로 분리한다.

### 4.2 App shell

#### Desktop, 1280px 이상

- 좌측 rail: `240px` expanded, 사용자가 접었을 때 `72px`
- 상단 utility bar: `64–72px`
- 본문: `max-width: 1440px`, 12-column grid, gutter `24px`
- 페이지 여백: `32–48px`
- 사용자 프로필, 알림, 도움말은 상단 우측 utility 영역에 둔다.
- 핵심 메뉴는 문자 라벨을 항상 제공한다. `H`, `P`, `M`, `S` 같은 이니셜만으로 의미를 대신하지 않는다.

#### Page hierarchy

모든 주요 화면은 다음 순서로 구성한다.

1. **Context:** 현재 위치, 제목, 목표
2. **Primary action:** 지금 이어서 할 한 가지
3. **Work queue:** 마감·피드백·미완료 활동
4. **Progress evidence:** 진행률, 결과물, 역량 변화
5. **Explore:** 추천 학습·커뮤니티·부가 기능

홈 첫 뷰포트에서는 `Primary action`과 긴급한 `Work queue`가 스크롤 없이 보여야 한다.

### 4.3 Home layout

- 환영 문구보다 오늘의 학습 맥락을 먼저 전달한다.
- Hero/mission card는 다음 정보를 포함한다: 과정명, 현재 단계, 예상 소요 시간, 마감, 중단 지점, CTA.
- 연속 학습, XP, 배지는 Hero를 보조하며 CTA보다 강하게 표현하지 않는다.
- `프로젝트`, `제출 산출물`, `평가 요청`, `이번 주 완료` 같은 요약 카드에는 클릭 후 무엇이 나오는지 동사형 보조 라벨을 제공한다.
- 모든 데이터가 배경 지도 위에 떠 있는 구조보다, 불투명한 작업 패널 안에서 비교 가능하도록 정렬한다.

### 4.4 Learning layout

#### VOD

- 데스크톱: 영상/실습 `8 columns` + 목차/자료/진도 `4 columns`
- 영상 아래 탭 순서: `개요 → 자료 → 퀴즈/코딩 → 질문`
- 자료, 교안, 코드 파일은 영상과 같은 학습 단계에 연결한다.
- 영상 시청률만으로 완료 처리하지 않는다. 퀴즈, 코딩, 확인 활동을 별도 상태로 표시한다.

#### PBL

- 상단: 프로젝트 목표, 역할, 팀, 마감, 전체 단계
- 본문: 현재 미션과 산출물
- 우측 또는 하단 보조 영역: 팀 활동, AI 도움, 동료평가, 피드백
- 단계는 `시작 전 / 진행 중 / 제출됨 / 피드백 대기 / 수정 필요 / 완료`로 명확히 구분한다.
- 지도형 로드맵은 순서와 잠금 이유가 명확할 때만 사용하고, 같은 내용을 접근 가능한 목록으로도 제공한다.

### 4.5 Dashboard layout

- 대시보드는 “멋진 숫자 모음”이 아니라 의사결정 도구다.
- 상단에는 기간 필터와 핵심 요약 3–4개만 둔다.
- 차트마다 사용자가 취할 수 있는 다음 행동을 연결한다.
- 최근 학습 목록, 주간/연간 현황, 성적, 수료증, 배지, 스킬, 크레딧은 동일한 시각적 강도를 갖지 않는다.
- 우선순위는 `미완료/피드백 필요 → 진행 중 → 성과 증거 → 보상` 순이다.

---

## 5. Component Rules

### 5.1 Navigation item

- 구조: 아이콘 `20px` + 라벨 + 선택 indicator + 선택적 badge
- 최소 높이 `44px`; rail 내부 권장 높이 `48px`
- 현재 위치는 라임 배경/indicator, 굵기, `aria-current="page"`로 함께 표시한다.
- hover와 selected 상태를 같게 만들지 않는다.
- 아이콘만 남는 collapsed rail에는 툴팁과 접근 가능한 이름을 제공한다.

### 5.2 Button

| Variant | Usage |
|---|---|
| Primary | 화면의 다음 핵심 행동 1개 |
| Secondary | 같은 작업의 보조 행동 |
| Tertiary | 낮은 우선순위 탐색·텍스트 행동 |
| Danger | 삭제·탈퇴·제출 취소 등 복구가 어려운 행동 |

- 기본 높이 `44px`, 중요한 CTA `48–52px`
- 아이콘만 있는 버튼도 최소 `44 × 44px`
- 로딩 중에는 너비를 유지하고 중복 실행을 막는다.
- disabled는 이유를 인접 텍스트나 툴팁으로 설명한다.
- `→` 장식만으로 링크임을 알리지 않는다.

### 5.3 Mission / Resume card

- 카드 전체를 클릭 가능하게 만들더라도 CTA 라벨을 명시한다.
- 필수 정보: 제목, 학습 유형, 현재 단계, 예상 시간, 마감, 진행률, 마지막 활동, 다음 행동.
- 진행률은 `38%`만 표시하지 않고 `5/13 단계`처럼 분모를 함께 보여 준다.
- 중단 지점을 기억해 `03:42부터 이어보기`, `2단계 미션 계속하기`처럼 복귀 비용을 줄인다.

### 5.4 Content card

- 썸네일은 장식이 아니라 콘텐츠 종류와 난이도를 파악하도록 돕는다.
- 목록에서 동일한 메타데이터 순서를 유지한다: 유형 → 제목 → 역량/주제 → 시간 → 상태.
- hover에만 핵심 정보가 나타나지 않게 한다.
- 잠긴 콘텐츠는 잠금 이유와 해제 조건을 함께 표시한다.

### 5.5 Progress

- progress bar에는 텍스트 라벨, 현재값, 최댓값을 제공한다.
- 연속형 진도는 bar, 단계형 진도는 stepper를 사용한다.
- `수강률`, `활동 완료율`, `프로젝트 진행률`, `역량 숙련도`를 같은 “진행률”로 합치지 않는다.
- 시간 기반 데이터와 성취 기반 데이터를 분리한다.

### 5.6 Status badge

- badge는 1–3단어로 제한한다.
- 의미 조합: 색 + 아이콘/형태 + 텍스트.
- 상태어를 표준화한다: `예정`, `진행 중`, `제출 필요`, `피드백 대기`, `수정 필요`, `완료`.
- 보상 badge와 작업 상태 badge의 형태와 색을 구분한다.

### 5.7 Data card and chart

- 카드 제목은 측정 대상과 기간을 포함한다.
- 큰 숫자 옆에 기준 또는 변화량을 제공한다.
- 차트 색은 최대 5개 범주를 기본으로 하고, 패턴·라벨·범례를 병행한다.
- hover tooltip에만 값을 숨기지 않는다.
- 표 형태의 대체 데이터 또는 요약 문장을 제공한다.

### 5.8 Form, quiz, coding, submission

- 라벨을 placeholder로 대체하지 않는다.
- 오류는 해당 필드 근처에 원인과 해결 방법을 쓴다.
- 퀴즈 제출 전/후 상태를 명확히 구분하고, 정답 색상만으로 결과를 전달하지 않는다.
- 코드 실행과 최종 제출을 시각적으로 분리한다.
- 파일 제출은 허용 형식, 용량, 업로드 진행, 성공, 실패, 재시도 상태를 제공한다.
- 자동 저장 시 마지막 저장 시간을 표시한다.

### 5.9 AI chat

- AI 도움은 학습을 대신하는 답안 생성기가 아니라 힌트, 설명, 검토를 제공하는 보조 도구로 포지셔닝한다.
- AI 메시지에는 출처, 한계, 재확인 필요성을 상황에 따라 표시한다.
- 프로젝트/강의 맥락을 참조했는지 사용자가 알 수 있게 한다.
- 대화 기록 삭제, 새 대화, 복사, 피드백 기능을 키보드로도 사용할 수 있게 한다.
- AI 답변 스트리밍은 스크린리더에 과도하게 반복 공지하지 않고 완료 시 요약 공지를 제공한다.

### 5.10 Modal and notification

- 파괴적이거나 흐름을 중단해야 하는 결정만 modal로 처리한다.
- 일반적인 상세보기와 보조 정보는 drawer 또는 inline expansion을 우선한다.
- toast는 결과 요약용이며 오류 복구에 필요한 내용을 toast에만 두지 않는다.
- 알림은 `마감`, `피드백`, `팀 요청`, `시스템`으로 구분하고 읽음 여부보다 행동 필요 여부를 우선한다.

---

## 6. Interaction

### 6.1 Interaction principles

- **Direct:** 클릭 결과를 라벨에서 예측할 수 있어야 한다.
- **Recoverable:** 자동 저장, undo, 버전 또는 확인 단계를 제공한다.
- **Context-preserving:** VOD/코딩/PBL에서 이동 후 돌아와도 위치와 입력을 복원한다.
- **Progressive:** 처음에는 다음 행동을 보여 주고, 상세 지표는 요청 시 확장한다.
- **Calm:** 장식적 모션이 읽기와 작업을 방해하지 않는다.

### 6.2 State model

모든 인터랙티브 컴포넌트는 다음 상태를 설계한다.

`default → hover → focus-visible → active → loading → success/error → disabled`

데이터 영역은 다음 상태를 별도로 설계한다.

`loading / empty / partial / complete / stale / permission denied / offline`

### 6.3 Motion

- hover/focus: `120–200ms`
- panel/drawer: `200–360ms`
- 페이지 전환은 opacity와 8–16px 이동 범위 안에서 절제한다.
- 지도 카메라 이동, 배경 입자, parallax는 홈과 로드맵에서만 사용한다.
- 자동 재생 영상은 기본 음소거이며 사용자가 멈출 수 있어야 한다.
- `prefers-reduced-motion`에서는 parallax, 입자, 카메라 이동, glow pulse를 제거하고 즉시 전환 또는 짧은 fade로 대체한다.

### 6.4 Feedback timing

- 즉시 반응이 가능한 클릭은 100ms 안에 상태 변화를 보여 준다.
- 1초 이상 작업은 spinner/skeleton, 10초 이상 작업은 단계 또는 예상 상태를 보여 준다.
- 제출 성공 후 결과와 다음 행동을 같은 화면에서 제공한다.
- 네트워크 실패 시 입력값을 보존하고 재시도 방법을 제공한다.

---

## 7. Responsive Rules

### 7.1 Breakpoints

| Name | Range | Behavior |
|---|---|---|
| Compact | `< 768px` | 1 column, bottom navigation, task-first |
| Medium | `768–1199px` | 8-column grid, collapsible rail |
| Wide | `≥ 1200px` | 12-column grid, persistent rail |
| Max | `≥ 1600px` | 콘텐츠 최대 폭 유지, 여백 증가 |

콘텐츠가 깨지는 지점에서 breakpoint를 조정하며 특정 기기 모델에 맞추지 않는다.

### 7.2 Compact/mobile

- 좌측 sidebar를 축소 복제하지 않는다.
- 하단 내비게이션은 `홈 / 학습 / 프로젝트 / 나` 4개를 기본으로 하고 커뮤니티는 `더보기` 또는 상단 진입점을 검토한다.
- 주요 CTA는 콘텐츠를 가리지 않는 범위에서 하단 sticky action으로 제공할 수 있다.
- 카드 그리드는 1열로 전환하고 정보 우선순위를 유지한다.
- VOD는 영상 → 현재 활동 → 목차/자료 순으로 재배치한다.
- PBL의 팀/피드백 패널은 tab 또는 bottom sheet로 전환한다.
- 시네마틱 배경은 정적 이미지 또는 단순 gradient로 경량화하고 텍스트 대비를 우선한다.
- 가로 스크롤은 코드, 표, 로드맵처럼 본질적으로 필요한 영역에만 허용하며 대체 요약을 제공한다.

### 7.3 Medium/tablet

- rail은 기본 collapsed 상태로 둘 수 있으나 현재 메뉴명은 페이지 헤더에 노출한다.
- 주요 작업과 보조 패널을 `5+3` 또는 `6+2` column으로 구성한다.
- dashboard KPI는 2열, 학습 목록은 1–2열로 제한한다.

### 7.4 Zoom and reflow

- 320 CSS px에서 핵심 흐름이 가로 스크롤 없이 동작해야 한다.
- 200% zoom에서 메뉴, 제목, CTA, 폼 오류가 겹치거나 잘리지 않아야 한다.
- 고정 높이 카드보다 `min-height`와 콘텐츠 기반 확장을 사용한다.
- 텍스트 크기에 `vw`만 사용하지 않고 `clamp()`의 최소/최댓값을 둔다.

---

## 8. Accessibility

목표는 **WCAG 2.2 AA**다. 이 문서는 목표와 규칙을 정의하며, 실제 준수 여부는 구현 후 자동·수동 테스트로 검증한다.

### 8.1 Perceivable

- 일반 텍스트 대비 최소 `4.5:1`, 큰 텍스트와 핵심 UI 경계는 최소 `3:1`.
- 배경 이미지/영상 위 텍스트는 프레임별 대비 변화에 영향받지 않도록 scrim을 사용한다.
- 색만으로 단계, 정답, 경고, 팀 상태, 차트 범주를 전달하지 않는다.
- VOD에 자막, 대본, 재생 속도, 키보드 제어를 제공한다.
- 중요한 이미지와 로드맵에는 의미 있는 대체 텍스트 또는 동등한 목록을 제공한다.

### 8.2 Operable

- 모든 기능은 키보드만으로 사용할 수 있어야 한다.
- `focus-visible`은 2px 이상, 주변과 `3:1` 이상 대비를 확보하고 glow만으로 표현하지 않는다.
- 최소 터치 목표 `44 × 44px`; 인접 목표 사이 충분한 간격을 둔다.
- skip link를 제공하고 랜드마크를 `header/nav/main/aside/footer`로 구분한다.
- sticky header와 bottom action이 포커스 요소나 오류 메시지를 가리지 않게 한다.
- 시간 제한이 있는 퀴즈/평가는 사전 고지와 연장 정책을 제공한다.

### 8.3 Understandable

- 같은 기능은 화면마다 같은 이름을 사용한다.
- 현재 단계, 완료 조건, 잠금 이유, 제출 결과를 명시한다.
- 폼 오류는 원인, 위치, 해결 방법을 제공한다.
- AI 추천과 평가에는 추천/판정의 근거와 사람에게 문의할 경로를 제공한다.
- 게이미피케이션 용어 옆에 실제 학습 의미를 병기한다.

### 8.4 Robust

- 메뉴, tab, accordion, dialog, progress, status는 올바른 HTML 요소와 ARIA 패턴을 사용한다.
- 진행률은 접근 가능한 이름과 현재/최대값을 제공한다.
- 동적 저장, 제출, 오류, AI 응답 완료는 적절한 live region으로 공지한다.
- DOM 순서는 시각적 순서와 일치해야 하며 CSS 재배치로 읽기 순서를 왜곡하지 않는다.

### 8.5 Required validation

- 키보드 전체 흐름: 로그인 → 홈 → VOD/PBL 진입 → 활동 → 제출 → 피드백
- 스크린리더: VoiceOver + Safari, NVDA + Chrome 중 최소 조합
- 대비 및 강제 색상 모드
- 200% zoom, 320px reflow
- `prefers-reduced-motion`
- 자막/대본과 영상 플레이어 제어
- 오류, 빈 상태, 느린 네트워크, 오프라인 복구

---

## 9. Anti-patterns

다음 패턴은 MILI AI의 학습 목표와 충돌하므로 금지하거나 강하게 제한한다.

1. **Neon everywhere:** 라임을 배경, 카드, 텍스트, 차트, CTA에 동시에 사용해 우선순위를 없애는 것.
2. **Atmosphere over legibility:** 움직이는 지형·입자·glow 때문에 본문과 상태가 흐려지는 것.
3. **Glass without contrast:** 배경이 그대로 비쳐 텍스트 대비가 장면마다 달라지는 패널.
4. **Dashboard as wallpaper:** 많은 숫자를 보여 주지만 사용자가 다음에 할 일을 알 수 없는 대시보드.
5. **Progress without meaning:** `38%`, `Lv.6`, `620 XP`만 있고 무엇을 완료했는지 설명하지 않는 것.
6. **Gamification replacing learning:** 배지와 연속 학습이 역량, 결과물, 피드백보다 더 중요한 것처럼 보이는 것.
7. **Map as mandatory navigation:** 모든 사용자가 3D 지도에서 목적지를 찾아야만 콘텐츠에 접근할 수 있게 하는 것.
8. **Mystery icons and initials:** `H/P/M/S`, 아이콘, 화살표만으로 메뉴와 행동을 표현하는 것.
9. **Too many primary actions:** 한 카드 또는 한 화면에 라임 Primary 버튼을 여러 개 배치하는 것.
10. **Color-only status:** 정답/오답, 완료/진행, 사람/AI 출처를 색만으로 구분하는 것.
11. **Hover-only information:** 마감, 설명, CTA, 차트 값이 hover에서만 보이는 것.
12. **Cinematic work screens:** 코딩, 퀴즈, 긴 글, 제출 화면까지 풀스크린 배경 영상과 parallax를 유지하는 것.
13. **Desktop shrunk to mobile:** sidebar와 다열 dashboard를 비율만 줄여 모바일에 넣는 것.
14. **Hidden recovery:** 자동 저장 실패, 업로드 실패, 오답 후 재시도 방법이 보이지 않는 것.
15. **AI without provenance:** AI 생성 답변과 사람의 피드백을 같은 방식으로 표시하는 것.
16. **Punitive copy:** 학습 실패를 경고·탈락·패널티 중심으로 표현하는 것.
17. **Decorative microcopy:** 작은 영문 대문자와 넓은 자간을 핵심 안내에 사용해 판독성을 낮추는 것.
18. **Inconsistent completion:** VOD, 퀴즈, 코딩, PBL마다 완료 기준이 다르지만 사용자에게 설명하지 않는 것.

---

## 10. Screen-specific Application Criteria

### 10.1 Landing

- 브랜드 메시지와 1개의 CTA만 둔다.
- 배경은 강하게 사용할 수 있으나 CTA와 본문 대비를 고정한다.
- 로그인 사용자는 랜딩을 반복 통과시키지 않고 홈 또는 마지막 학습으로 연결한다.

### 10.2 Home

- 첫 화면의 최우선 정보는 `오늘의 다음 행동`.
- `마감 임박`, `피드백 도착`, `팀 요청`이 있으면 일반 추천보다 앞선다.
- 시네마틱 배경은 사용자의 진행 경로와 연결될 때만 유지한다.

### 10.3 VOD/PBL

- 세계관보다 작업 집중을 우선하는 불투명 표면을 사용한다.
- 학습 단계, 완료 조건, 저장 상태, 도움 경로가 항상 보이게 한다.
- VOD와 PBL을 별개의 메뉴로만 두지 않고 선수 학습과 적용 프로젝트의 관계를 보여 준다.

### 10.4 Community

- 콘텐츠 종류보다 사용자의 목적을 강조한다: `질문하기`, `답변 기다리는 글`, `해결된 글`.
- 동료평가와 일반 게시글의 권한·공개 범위를 구분한다.
- AI 답변과 사용자 답변을 명확히 구분한다.

### 10.5 Personal dashboard

- 학습 시간보다 완료 활동, 결과물, 피드백 반영, 역량 변화를 더 중요하게 보여 준다.
- 수료증·배지·크레딧은 증거와 보상을 구분해 배치한다.
- 역량진단 결과는 점수로 끝나지 않고 관련 강의/PBL/로드맵의 다음 단계로 연결한다.

---

## 11. Open Decisions for Validation

이 초안은 제공된 메뉴 맵과 정적 시안을 기반으로 한다. 다음 항목은 실제 사용자·콘텐츠·기술 조건을 확인한 뒤 확정한다.

- 핵심 사용자군과 사용 환경: 직장인/학생/군 조직 등
- `부대별 리더보드`, 레벨, XP, 크레딧의 실제 학습 목적과 노출 정책
- VOD, 퀴즈, 코딩, PBL의 정확한 완료 조건
- 동료평가 익명성, 팀 권한, 리더 권한
- AI 채팅이 접근하는 강의·프로젝트 데이터 범위
- 홈의 우선순위 계산 규칙: 마감, 학습 추천, 팀 요청, 피드백
- 모바일에서 커뮤니티를 1차 내비게이션에 포함할지 여부
- 제공된 ERD의 권한·상태 모델과 화면 상태의 대응 관계
- 브랜드 폰트, 로고 clear space, 이미지/3D asset 제작 규격

---

## 12. Definition of Done for UI

새 화면이나 컴포넌트는 다음을 만족해야 완료로 본다.

- 사용자의 핵심 질문과 Primary Action이 5초 안에 파악된다.
- loading, empty, error, disabled, success 상태가 설계되어 있다.
- 키보드와 screen reader용 이름·순서·상태가 정의되어 있다.
- 320px, 768px, 1280px, 1600px 및 200% zoom에서 검토했다.
- 텍스트/배경/경계 대비를 실제 도구로 측정했다.
- 배경 이미지가 없어도 핵심 정보와 흐름이 유지된다.
- 진행률의 대상, 현재값, 완료 기준이 설명된다.
- AI 생성 내용과 사람의 콘텐츠가 구분된다.
- 학습 중단 후 복귀 위치와 저장 상태가 유지된다.
- 장식적 모션을 제거해도 기능과 의미가 손상되지 않는다.

